/* ========================================================================
   EXTRACTED FROM EFFECTS-DASHBOARD.HTML - MODERN SIDEBAR EDITION
   ======================================================================== */

/* ========================================================================
   CSS VARIABLES
   ======================================================================== */
:root {
  /* Original color scheme */
  --bg-0: #050608;
  --bg-1: #0b0f1a;
  --bg-2: #121826;
  --panel: rgb(18 24 38 / 0.82);
  --panel-strong: rgb(21 30 48 / 0.94);
  --border: rgb(118 145 183 / 0.18);
  --text: #eef2f8;
  --muted: #9aa4b6;
  --accent: #f59e0b;
  --accent-2: #2dd4bf;
  --danger: #f97316;
  --shadow: rgb(0 0 0 / 0.35);

  /* Sidebar */
  --sidebar-width: 320px;
  --sidebar-collapsed-width: 64px;

  /* Z-index layers */
  --z-sidebar: 1000;
  --z-backdrop: 999;
  --z-header: 100;
}

/* ========================================================================
   RESET & BASE
   ======================================================================== */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  overflow-x: hidden;
  font-size: 16px;
}

body {
  font-family: "Avenir Next", "Gill Sans", "Trebuchet MS", sans-serif;
  background:
    radial-gradient(900px circle at 10% 10%, rgb(45 212 191 / 0.1), transparent 55%),
    radial-gradient(900px circle at 85% -10%, rgb(245 158 11 / 0.18), transparent 60%),
    linear-gradient(180deg, var(--bg-1) 0%, var(--bg-0) 100%);
  color: var(--text);
  min-height: 100vh;
  overflow-x: hidden;
  position: relative;
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  background-image:
    linear-gradient(rgb(255 255 255 / 0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgb(255 255 255 / 0.04) 1px, transparent 1px);
  background-size: 60px 60px;
  pointer-events: none;
  opacity: 0.25;
  z-index: 0;
}

/* ========================================================================
   APP CONTAINER
   ======================================================================== */
.app {
  display: flex;
  min-height: 100vh;
  position: relative;
  overflow-x: hidden;
  max-width: 100vw;
}

/* ========================================================================
   SIDEBAR
   ======================================================================== */
.sidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  width: var(--sidebar-width);
  background: var(--panel-strong);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  transition:
    transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
    width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: var(--z-sidebar);
  backdrop-filter: blur(12px);
  overflow: hidden;
  box-shadow: 4px 0 20px var(--shadow);
}

.sidebar.collapsed {
  width: var(--sidebar-collapsed-width);
}

.sidebar-toggle-btn {
  position: absolute;
  top: 20px;
  right: 16px;
  width: 36px;
  height: 36px;
  background: rgb(255 255 255 / 0.05);
  border: 1px solid var(--border);
  border-radius: 10px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
  color: var(--muted);
  z-index: 10;
}

.sidebar-toggle-btn:hover {
  background: rgb(255 255 255 / 0.1);
  border-color: rgb(118 145 183 / 0.35);
  color: var(--text);
  transform: scale(1.05);
}

.sidebar-toggle-btn:active {
  transform: scale(0.95);
}

.sidebar-toggle-btn svg {
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.sidebar.collapsed .sidebar-toggle-btn svg {
  transform: rotate(180deg);
}

.sidebar-header {
  padding: 24px 20px 20px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-logo {
  display: flex;
  align-items: center;
  gap: 12px;
  color: var(--accent-2);
  transition: all 0.3s ease;
}

.sidebar.collapsed .sidebar-logo {
  justify-content: center;
}

.sidebar.collapsed .sidebar-logo svg {
  margin: 0;
}

.sidebar.collapsed .sidebar-title {
  opacity: 0;
  width: 0;
  overflow: hidden;
}

.sidebar-title {
  font-size: 19px;
  font-weight: 700;
  color: var(--text);
  white-space: nowrap;
  letter-spacing: 0.02em;
  transition:
    opacity 0.3s ease,
    width 0.3s ease;
}

.sidebar-content {
  flex: 1;
  overflow: hidden auto;
  padding: 16px 0;
}

.sidebar-content::-webkit-scrollbar {
  width: 6px;
}

.sidebar-content::-webkit-scrollbar-track {
  background: transparent;
}

.sidebar-content::-webkit-scrollbar-thumb {
  background: rgb(255 255 255 / 0.1);
  border-radius: 3px;
}

.sidebar-content::-webkit-scrollbar-thumb:hover {
  background: rgb(255 255 255 / 0.2);
}

.sidebar-section {
  margin-bottom: 12px;
  padding: 0 12px;
}

.sidebar-section-title {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 14px;
  font-size: 12px;
  font-weight: 700;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  cursor: pointer;
  user-select: none;
  border-radius: 10px;
  transition: all 0.2s ease;
}

.sidebar-section-title:hover {
  background: rgb(255 255 255 / 0.05);
  color: var(--text);
}

.sidebar.collapsed .sidebar-section-title span {
  opacity: 0;
  width: 0;
  overflow: hidden;
}

.sidebar.collapsed .sidebar-section-title {
  justify-content: center;
  padding: 12px;
}

.sidebar-section-content {
  padding: 12px 14px 20px;
  transition:
    opacity 0.3s ease,
    max-height 0.3s ease,
    padding 0.3s ease;
}

.sidebar.collapsed .sidebar-section-content {
  opacity: 0;
  max-height: 0;
  padding: 0;
  overflow: hidden;
}

/* ========================================================================
   PLAYBACK CONTROLS
   ======================================================================== */
.playback-buttons {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 16px;
}

.now-playing {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 12px;
  background: rgb(45 212 191 / 0.08);
  border: 1px solid rgb(45 212 191 / 0.15);
  border-radius: 10px;
  opacity: 0.6;
  transition: all 0.3s ease;
}

.now-playing[data-active="true"] {
  opacity: 1;
  background: rgb(45 212 191 / 0.12);
  border-color: rgb(45 212 191 / 0.3);
  box-shadow: 0 0 20px rgb(45 212 191 / 0.15);
}

.now-dot {
  width: 8px;
  height: 8px;
  background: var(--muted);
  border-radius: 50%;
  flex-shrink: 0;
  transition: all 0.3s ease;
}

.now-playing[data-active="true"] .now-dot {
  background: var(--accent-2);
  box-shadow: 0 0 8px var(--accent-2);
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%,
  100% {
    opacity: 1;
    transform: scale(1);
  }

  50% {
    opacity: 0.6;
    transform: scale(0.9);
  }
}

.now-playing-content {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 0;
  flex: 1;
}

.now-playing-label {
  font-size: 10px;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  font-weight: 600;
}

.now-playing-content strong {
  font-size: 13px;
  color: var(--text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-weight: 600;
}

/* ========================================================================
   PERSISTENCE CONTROLS
   ======================================================================== */
.persistence-buttons {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.file-input-label {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 11px 16px;
  background: rgb(255 255 255 / 0.05);
  border: 1px solid var(--border);
  border-radius: 10px;
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: center;
}

.file-input-label:hover {
  background: rgb(255 255 255 / 0.1);
  border-color: rgb(118 145 183 / 0.35);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px var(--shadow);
}

.file-input-label:active {
  transform: translateY(0);
}

.file-input {
  display: none;
}

.file-input-label svg {
  flex-shrink: 0;
}

/* ========================================================================
   SIDEBAR BACKDROP (MOBILE)
   ======================================================================== */
.sidebar-backdrop {
  position: fixed;
  inset: 0;
  background: rgb(0 0 0 / 0.6);
  z-index: var(--z-backdrop);
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
  backdrop-filter: blur(4px);
}

.sidebar-backdrop:not([hidden]) {
  opacity: 1;
  pointer-events: all;
}

/* ========================================================================
   MAIN WRAPPER
   ======================================================================== */
.main-wrapper {
  flex: 1;
  margin-left: var(--sidebar-width);
  transition: margin-left 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-width: 0;
  max-width: calc(100vw - var(--sidebar-width));
  overflow-x: hidden;
}

.sidebar.collapsed + .sidebar-backdrop + .main-wrapper {
  margin-left: var(--sidebar-collapsed-width);
  max-width: calc(100vw - var(--sidebar-collapsed-width));
}

/* ========================================================================
   HEADER
   ======================================================================== */
.header {
  padding: 40px 32px 32px;
  border-bottom: 1px solid var(--border);
  background: rgb(11 15 26 / 0.6);
  backdrop-filter: blur(12px);
  position: sticky;
  top: 0;
  z-index: var(--z-header);
}

.header-content h1 {
  font-family: Oswald, Impact, "Franklin Gothic Medium", sans-serif;
  font-size: 2.6rem;
  letter-spacing: 0.06em;
  margin-bottom: 12px;
  background: linear-gradient(135deg, var(--accent-2) 0%, var(--accent) 100%);
  background-clip: text;
  -webkit-background-clip: text; /* stylelint-disable-line property-no-vendor-prefix */
  -webkit-text-fill-color: transparent;
}

.header-content p {
  font-size: 15px;
  color: var(--muted);
  max-width: 900px;
  line-height: 1.6;
}

/* ========================================================================
   CONTENT GRID
   ======================================================================== */
.grid {
  display: grid;
  gap: 18px;
}

.content-grid {
  padding: 28px 32px 48px;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
}

.span-2 {
  grid-column: 1 / -1;
}

/* ========================================================================
   PANELS
   ======================================================================== */
.panel {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: 14px;
  padding: 24px;
  backdrop-filter: blur(8px);
  transition:
    border-color 0.25s,
    background 0.25s,
    box-shadow 0.25s,
    transform 0.25s;
  animation: fade-slide-in 0.35s ease forwards;
  animation-delay: var(--delay, 0ms);
  opacity: 0;
}

@keyframes fade-slide-in {
  from {
    opacity: 0;
    transform: translateY(12px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.panel:hover {
  background: var(--panel-strong);
  border-color: rgb(118 145 183 / 0.3);
  box-shadow: 0 8px 24px var(--shadow);
  transform: translateY(-2px);
}

.panel h2 {
  font-family: Oswald, Impact, "Franklin Gothic Medium", sans-serif;
  font-size: 1.4rem;
  letter-spacing: 0.05em;
  margin-bottom: 8px;
}

.panel-note {
  color: var(--muted);
  font-size: 0.88rem;
  line-height: 1.5;
  margin-bottom: 18px;
}

/* ========================================================================
   BUTTONS
   ======================================================================== */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 11px 18px;
  font-family: inherit;
  font-size: 14px;
  font-weight: 600;
  border: 1px solid var(--border);
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.2s ease;
  background: rgb(255 255 255 / 0.05);
  color: var(--text);
  white-space: nowrap;
  user-select: none;
}

.btn:hover:not(:disabled) {
  background: rgb(255 255 255 / 0.1);
  border-color: rgb(118 145 183 / 0.35);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px var(--shadow);
}

.btn:active:not(:disabled) {
  transform: translateY(0);
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn svg {
  flex-shrink: 0;
}

.btn-primary {
  background: linear-gradient(135deg, var(--accent-2), var(--accent));
  border-color: transparent;
  color: #000;
  font-weight: 700;
}

.btn-primary:hover:not(:disabled) {
  background: linear-gradient(135deg, #38e5cf, #ffa726);
  box-shadow:
    0 4px 16px rgb(45 212 191 / 0.4),
    0 0 24px rgb(245 158 11 / 0.2);
  transform: translateY(-2px);
}

.btn-secondary {
  background: rgb(255 255 255 / 0.05);
  border-color: var(--border);
}

.btn-accent {
  background: var(--accent);
  border-color: transparent;
  color: #000;
  font-weight: 700;
}

.btn-accent:hover:not(:disabled) {
  background: #ffa726;
  box-shadow: 0 4px 16px rgb(245 158 11 / 0.4);
}

.btn.ghost {
  background: transparent;
  border-color: rgb(118 145 183 / 0.25);
}

.btn.ghost:hover:not(:disabled) {
  background: rgb(255 255 255 / 0.08);
}

.btn-play {
  width: 100%;
}

/* ========================================================================
   FORM ELEMENTS
   ======================================================================== */
.row {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  align-items: flex-start;
}

.field {
  display: flex;
  flex-direction: column;
  gap: 6px;
  flex: 1;
  min-width: 140px;
}

.field label {
  font-size: 0.88rem;
  font-weight: 600;
  color: var(--muted);
}

.field input,
.field select {
  padding: 10px 12px;
  font-family: inherit;
  font-size: 14px;
  background: rgb(255 255 255 / 0.05);
  border: 1px solid var(--border);
  border-radius: 8px;
  color: var(--text);
  transition: all 0.2s ease;
}

.field input:focus,
.field select:focus {
  outline: none;
  border-color: var(--accent-2);
  background: rgb(255 255 255 / 0.08);
  box-shadow: 0 0 0 3px rgb(45 212 191 / 0.15);
}

/* Toggle switches */
.toggle {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  user-select: none;
}

.toggle input[type="checkbox"] {
  appearance: none;
  width: 42px;
  height: 24px;
  background: rgb(255 255 255 / 0.1);
  border: 1px solid var(--border);
  border-radius: 12px;
  position: relative;
  cursor: pointer;
  transition: all 0.3s ease;
}

.toggle input[type="checkbox"]::before {
  content: "";
  position: absolute;
  width: 18px;
  height: 18px;
  background: var(--text);
  border-radius: 50%;
  top: 2px;
  left: 2px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.toggle input[type="checkbox"]:checked {
  background: var(--accent-2);
  border-color: var(--accent-2);
}

.toggle input[type="checkbox"]:checked::before {
  left: 20px;
  background: #000;
}

.toggle span {
  font-size: 14px;
  font-weight: 500;
  color: var(--text);
}

/* Radio groups */
.radio-section {
  margin-top: 14px;
}

.radio-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 10px;
}

.radio-grid.no-scroll {
  overflow: visible;
}

.radio-item {
  position: relative;
}

.radio-item input[type="radio"] {
  appearance: none;
  position: absolute;
  opacity: 0;
}

.radio-item label {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 14px;
  background: rgb(255 255 255 / 0.05);
  border: 1px solid var(--border);
  border-radius: 10px;
  cursor: pointer;
  transition: all 0.2s ease;
  font-size: 14px;
  font-weight: 500;
}

.radio-item input[type="radio"]:checked + label {
  background: var(--accent-2);
  border-color: var(--accent-2);
  color: #000;
  font-weight: 700;
  box-shadow: 0 0 16px rgb(45 212 191 / 0.3);
}

.radio-item label:hover {
  background: rgb(255 255 255 / 0.1);
  border-color: rgb(118 145 183 / 0.35);
  transform: translateY(-1px);
}

.radio-item input[type="radio"]:checked + label::before {
  content: "";
  width: 6px;
  height: 6px;
  background: #000;
  border-radius: 50%;
  flex-shrink: 0;
}

/* Sliders */
.slider-row {
  display: flex;
  gap: 12px;
  align-items: center;
  margin-bottom: 14px;
}

.slider-row label {
  font-size: 0.88rem;
  font-weight: 600;
  color: var(--muted);
  min-width: 140px;
}

.slider-row input[type="range"] {
  flex: 1;
  height: 6px;
  background: rgb(255 255 255 / 0.1);
  border-radius: 3px;
  outline: none;
  appearance: none;
}

.slider-row input[type="range"]::-webkit-slider-thumb {
  appearance: none;
  width: 16px;
  height: 16px;
  background: var(--accent-2);
  border-radius: 50%;
  cursor: pointer;
  transition: all 0.2s ease;
}

.slider-row input[type="range"]::-webkit-slider-thumb:hover {
  background: #38e5cf;
  transform: scale(1.2);
  box-shadow: 0 0 12px var(--accent-2);
}

.slider-row input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  background: var(--accent-2);
  border: none;
  border-radius: 50%;
  cursor: pointer;
  transition: all 0.2s ease;
}

.slider-row input[type="range"]::-moz-range-thumb:hover {
  background: #38e5cf;
  transform: scale(1.2);
  box-shadow: 0 0 12px var(--accent-2);
}

.slider-row .value {
  font-size: 0.88rem;
  font-weight: 700;
  color: var(--accent-2);
  min-width: 50px;
  text-align: right;
}

/* ========================================================================
   RESPONSIVE
   ======================================================================== */

/* Tablets */
@media (width <= 900px) {
  .sidebar {
    transform: translateX(-100%);
  }

  .sidebar:not(.collapsed) {
    transform: translateX(0);
  }

  .main-wrapper {
    margin-left: 0;
    max-width: 100vw;
  }

  .sidebar.collapsed + .sidebar-backdrop + .main-wrapper {
    margin-left: 0;
  }

  .header {
    padding: 32px 24px 24px;
  }

  .header-content h1 {
    font-size: 2.2rem;
  }

  .content-grid {
    padding: 24px 20px 40px;
    grid-template-columns: 1fr;
  }
}

/* Mobile */
@media (width <= 600px) {
  .sidebar {
    width: 280px;
  }

  .sidebar.collapsed {
    width: var(--sidebar-collapsed-width);
  }

  .header {
    padding: 24px 16px 20px;
  }

  .header-content h1 {
    font-size: 1.8rem;
  }

  .header-content p {
    font-size: 14px;
  }

  .content-grid {
    padding: 20px 16px 32px;
    gap: 14px;
  }

  .panel {
    padding: 18px;
  }

  .panel h2 {
    font-size: 1.2rem;
  }

  .radio-grid {
    grid-template-columns: 1fr;
  }

  .slider-row {
    flex-direction: column;
    align-items: stretch;
    gap: 8px;
  }

  .slider-row label {
    min-width: auto;
  }

  .slider-row .value {
    text-align: left;
  }

  .btn {
    font-size: 13px;
    padding: 10px 14px;
  }
}
